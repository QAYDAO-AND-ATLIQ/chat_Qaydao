# Quality Guard — أنواع التنبيهات ولوحة تحكم المدير

> **الحالة:** في الإنتاج منذ 2026-07-12 · **الواجهة:** <https://chat.qaydao.com/quality-guard/> ← ⚙️ إعدادات الجودة ← 🛡️ قواعد التنبيهات
> **القيم في هذا الملف:** لقطة من قاعدة الإنتاج بتاريخ 2026-07-12 — المصدر الحي دائماً جدول `qg_alert_types`.

## المشكلة

- **97% من التنبيهات (1,388 من 1,430) كانت hardcoded** في كود Python — المدير لا يستطيع إيقاف أي منها أو تعديل خطورتها دون مبرمج و rebuild.
- **صفر حد للتكرار:** `customer_abuse` وصل **9 تنبيهات في محادثة واحدة** ⇒ إغراق ملاحظات داخلية ⇒ الموظفون يتجاهلون التنبيهات كلها.

## الحل — 4 طبقات

1. **جدول `qg_alert_types`** — 14 نوعاً، مصدر الحقيقة الوحيد (تشغيل/إيقاف، خطورة، حد تكرار، تهدئة، عتبة). الترحيل: [`migrations/001_alert_types.sql`](migrations/001_alert_types.sql).
2. **بوابة إنفاذ في `_store_alert()`** ([`app/app.py`](app/app.py)) — نقطة اختناق واحدة يمر بها كل تنبيه، **صفر تعديل على منطق الكشف** في المُصنِّفات.
3. **واجهة تحكم للمدير** — تبويب «قواعد التنبيهات» بمستويين: ① أنواع التنبيهات (الجدول) ← ② العبارات (مفلترة حسب النوع).
4. **مراقبة أسبوعية ذاتية** — [`../monitoring/qg_gate_monitor.py`](../monitoring/qg_gate_monitor.py): إيميل كل أحد 09:00 الرياض يكشف أي فشل للبوابة.

## الأنواع الـ 14 (قيم الإنتاج الحية — 2026-07-12)

| alert_type | الاسم العربي | الفئة | scope | مصدر الكشف | مفعّل | الخطورة | حد/محادثة |
|---|---|---|---|---|---|---|---|
| `first_response_delay` | تأخر أول رد (SLA) | معايير الشات | external | كود `sla.py` (عتبة 5 دقائق من الجدول) | ✅ | medium | 1 |
| `missing_greeting` | غياب الترحيب | معايير الشات | external | كود `classifier.py` | ✅ | low | 1 |
| `missing_closing_check` | غياب سؤال الختام | معايير الشات | external | كود `classifier.py` | ✅ | low | 0 (بلا حد) |
| `missing_rating_close` | غياب رسالة التقييم | معايير الشات | external | كود `classifier.py` | ✅ | low | 1 |
| `excessive_internal_notes` | كثرة الملاحظات الداخلية | معايير الشات | note | كود `app.py` (عتبة 5 من الجدول) | ✅ | medium | 0 (بلا حد) |
| `official_policy_mismatch` | مخالفة السياسة الرسمية | السياسات | external | كود `policy.py` | ⛔ معطّل | high | 0 (بلا حد) |
| `abuse` | إساءة للعميل | سلوك الموظف | external | عبارات DB (`qg_rules`) | ✅ | high | 2 |
| `unprofessional_reply` | رد غير مهني | سلوك الموظف | external | عبارات DB | ✅ | medium | 2 |
| `policy_risk` | وعد/رفض غير مؤكد | مخاطر | external | عبارات DB | ✅ | high | 0 (بلا حد) |
| `sales_risk` | أسلوب السعر والعروض | مخاطر | external | عبارات DB | ✅ | medium | 0 (بلا حد) |
| `delay_handling_risk` | تعامل مع تأخر الشحن | مخاطر | external | عبارات DB | ✅ | medium | 0 (بلا حد) |
| `unprofessional_note` | وصف مسيء بالنوت | ملاحظات داخلية | note | عبارات DB | ✅ | high | 0 (بلا حد) |
| `internal_argument` | جدال بين الموظفين | ملاحظات داخلية | note | عبارات DB | ✅ | medium | 0 (بلا حد) |
| `customer_abuse` | إساءة أو تهديد من العميل | سلوك العميل | customer | عبارات DB | ✅ | high | 1 |

## كيف يستخدمها الفريق

1. افتح <https://chat.qaydao.com/quality-guard/> ← تبويب **⚙️ إعدادات الجودة** (يتطلب مشرف Chatwoot).
2. تبويب **🛡️ قواعد التنبيهات** ← المستوى الافتراضي **① أنواع التنبيهات**.
3. **إيقاف/تفعيل نوع:** مفتاح «الحالة». إيقاف نوع بخطورة عالية يطلب تأكيدين.
4. **ضبط عدد مرات التكرار:** عمود «عدد مرات التكرار في المحادثة» — `0` = بلا حد، `1` = تنبيه واحد لكل محادثة. يوجد أيضاً «التهدئة (دقائق)» و«العتبة» (لـ SLA والملاحظات).
5. **العبارات:** زر «عبارات (N)» في أي صف يفتح المستوى ② مفلتراً على ذلك النوع.

- ⏱️ **التغيير يسري خلال ≤60 ثانية** (cache TTL) — **بلا rebuild وبلا إعادة تشغيل**.
- 🔒 **الأنواع النظامية (`is_system`) تُوقَف ولا تُحذف** — زر الحذف معطّل لها. الحذف متاح فقط للأنواع المخصصة المضافة يدوياً.
- 📝 كل تعديل يُسجَّل في «سجل التدقيق» (من، ماذا، قبل/بعد).

## قرارات معمارية (مع السبب)

**بوابة واحدة في `_store_alert` بدل تعديل 6 مُصنِّفات.** كل تنبيه في النظام — من المُصنِّفات أو SLA أو السياسات — يمر عبر `_store_alert`. وضع المنطق هناك يعني: لا تكرار للمنطق، أي نوع يُضاف مستقبلاً يخضع تلقائياً، ومنطق الكشف لم يُمَس ⇒ صفر خطر انحدار.

**fail-open عند غياب النوع (`at is None` ⇒ مرّر).** نوع غير معرّف في الجدول يجب ألا يختفي بصمت — وإسقاط الجدول كله (rollback) يعيد النظام لسلوكه الأصلي فوراً حتى قبل استرجاع الكود.

**`_store_alert` يرجع `None` عند القمع — وكل مستدعٍ يجب ألا ينشر نوت حينها.** هذه أخطر نقطة في التصميم: القمع يجب أن يقمع الملاحظة الداخلية أيضاً، لا التخزين فقط. المستدعون الأربعة (`app.py` ×3 + `sla.py` sweep) كلهم يشترطون `alert_id` فعلياً قبل النشر. **`sla.py` تحديداً** كان لا يلتقط القيمة الراجعة أصلاً وينشر النوت بلا شرط — أي مستدعٍ جديد يجب أن يلتزم بالعقد نفسه: **لا `alert_id` ⇒ لا نوت**.

**المراقبة تستنسخ شرط البوابة بأثر رجعي بدل عدّ الأعراض.** عدّ «تنبيهات المحادثة > الحد» في نافذة زمنية يلتقط إغراق ما قبل تفعيل الحدود كتجاوزات زائفة (30 نتيجة زائفة في أول dry-run). الكاشف الصحيح يعيد تقييم شرط البوابة نفسه (`seen >= cap` لحظة الإدخال) على كل صف خُزّن بعد التفعيل — أي نتيجة يرجعها هي بالتعريف خطأ برمجي في البوابة، فـ «صفر نتائج» ضمانة حقيقية.

**ثابت `GATE_ACTIVE_SINCE = 2026-07-12 07:37+00`** في `qg_gate_monitor.py` — لحظة تفعيل البوابة والحدود. **يجب تحديثه** إذا أُعيد ضبط الحدود جذرياً أو أُعيد بناء الجدول من الصفر، وإلا انحرف نطاق فحص التجاوزات.

## قيود تشغيلية

- الخدمة `build: ./app` **بلا bind-mount** ⇒ أي تعديل Python يتطلب rebuild:
  ```bash
  cd /root/chat-qaydao/quality-guard
  docker compose -f docker-compose.yml -p quality_guard build quality_guard
  docker compose -f docker-compose.yml -p quality_guard up -d --no-deps quality_guard
  ```
- ⚠️ **إلزامي دائماً `-f docker-compose.yml -p quality_guard`** — بدونهما يكتشف Docker ملف الأب في `/root/chat-qaydao/` ويدمج حاويات Chatwoot.
- `/health` على جذر التطبيق فقط: `curl http://127.0.0.1:8090/health` (المسارات العامة تحت بادئة `/quality-guard`).
- **`official_policy_mismatch` معطّل عمداً:** جدول `qg_policies` فارغ فعلياً (3 صفوف كلها `is_active=false`) ⇒ فحص السياسات no-op أصلاً. الإيقاف يعكس الواقع.

## المتبقي / فرص

- **8 أنواع بلا حد** (`max_per_conversation=0`) — تُضبط من الواجهة مباشرة عند ظهور إغراق؛ الإيميل الأسبوعي يقترحها تلقائياً عند `worst ≥ 3`.
- **`cooldown_minutes` جاهز بلا كود** — مثلاً `1440` = تنبيه واحد من النوع كل يوم لكل محادثة (مفيد للمحادثات الطويلة المعاد فتحها، لأن حد التكرار يُحسب على عمر المحادثة كله).
- **`official_policy_mismatch`:** البوابة والواجهة والكشف مبنية بالكامل — يحتاج فقط إدخال السياسات الرسمية الفعلية في `qg_policies` ثم تفعيل النوع من الواجهة.

## التراجع (Rollback)

```bash
# الكود: أعد البناء من التزام سابق
cd /root/chat-qaydao && git checkout 24d647c -- quality-guard/app/
cd quality-guard
docker compose -f docker-compose.yml -p quality_guard build quality_guard
docker compose -f docker-compose.yml -p quality_guard up -d --no-deps quality_guard

# الجدول (اختياري — الكود الجديد fail-open ويعمل بدونه بسلوك ما قبل البوابة)
docker exec quality_guard_db psql -U qguard -d quality_guard \
  -c "DROP TABLE IF EXISTS qg_alert_types;"
```

> بإسقاط الجدول وحده: `at is None` ⇒ fail-open ⇒ النظام يعود فوراً لسلوكه الأصلي حتى مع بقاء كود البوابة.
> نسخة كاملة قبل التغيير: `_backups_alerttypes_20260712_062933/` (dump + snapshot).

## سجل التغيير

| التزام | المحتوى |
|---|---|
| `3ed529a` | الجدول + بوابة الإنفاذ + تحصين المستدعين + العتبات الديناميكية + واجهة المستويين + الترحيل |
| `706a929` | المراقبة الأسبوعية `qg_gate_monitor.py` + cron الأحد 09:00 الرياض |
